---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: addons-bpp
spec:
  syncPolicy:
    preserveResourcesOnDeletion: false
  generators:
    - merge:
        mergeKeys: [server]
        generators:
          - clusters:
              values:
                addonChart: bpp
                # anything not staging or prod use this version
                addonChartVersion: 0.1.0
                addonChartRepositoryNamespace: bpp
              selector:
                matchExpressions:
                  - key: enable_beckn
                    operator: In
                    values: ['true']
          - clusters:
              selector:
                matchLabels:
                  environment: dev
              values:
                addonChartVersion: 0.1.0
  template:
    metadata:
      name: '{{values.addonChart}}'
      annotations:
        argocd.argoproj.io/sync-wave: "-5"  
        argocd.argoproj.io/manifest-generate-paths: .
    spec:
      project: default
      sources:
        - repoURL: '{{metadata.annotations.addons_repo_url}}'
          targetRevision: '{{metadata.annotations.addons_repo_revision}}'
          ref: values
        - path: bootstrap/beckn/{{values.addonChart}}
          repoURL: '{{metadata.annotations.addons_repo_url}}'
          targetRevision: '{{metadata.annotations.addons_repo_revision}}'
          helm:
            releaseName: '{{values.addonChart}}'
            ignoreMissingValueFiles: true
            valueFiles:
              - $values/{{metadata.annotations.addons_repo_basepath}}environments/default/addons/{{values.addonChart}}/values.yaml
            values: |
              global:
                namespace: {{values.addonChartRepositoryNamespace}}
                redisCache:
                  host: bpp-common-redis-master.bpp-common.svc.cluster.local
                  port: 6379
                responseCacheMongo:
                  host: bpp-common-mongodb.bpp-common.svc.cluster.local
                  port: 27017
                  dbname: protocol_server
                  username: "{{ metadata.annotations.bpp_mongo_auth_username }}"
                  password: "{{ metadata.annotations.bpp_mongo_auth_password }}"
                rabbitMQamqp:
                  host: bpp-common-rabbitmq.bpp-common.svc.cluster.local
                  port: 5672
                  username: "{{ metadata.annotations.bpp_rabbitmq_username }}"
                  password: "{{ metadata.annotations.bpp_rabbitmq_password }}"
                registry_url:  "registry-beckn.{{ metadata.annotations.domain }}"
                bpp:
                  privateKey: "{{ metadata.annotations.bpp_private_key }}" 
                  publicKey: "{{ metadata.annotations.bpp_public_key }}"
                externalDomain: "bpp-network.{{ metadata.annotations.domain }}" 
                webhookDomain: "bpp-webhook.{{ metadata.annotations.domain }}"          
              bpp-network:
                ingress:
                  enabled: true
                  annotations:
                    cert-manager.io/cluster-issuer: "{{ metadata.annotations.certificate_issuer }}"
                  ingressClassName: nginx
                  hosts:
                    - host: bpp-network.{{ metadata.annotations.domain }}
                      paths:
                        - /
                  tls:
                    - hosts:
                        - bpp-network.{{ metadata.annotations.domain }}
                      secretName: bpp-network-tls   
              bpp-client:
                ingress:
                  enabled: true
                  annotations:
                    cert-manager.io/cluster-issuer: "{{ metadata.annotations.certificate_issuer }}"
                  ingressClassName: nginx
                  hosts:
                    - host: bpp-client.{{ metadata.annotations.domain }}
                      paths:
                        - /
                  tls:
                    - hosts:
                        - bpp-client.{{ metadata.annotations.domain }}
                      secretName: bpp-client-tls   
    
      destination:
        namespace: '{{values.addonChartRepositoryNamespace}}'
        name: '{{name}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          # - ServerSideApply=true  # Big CRDs.
